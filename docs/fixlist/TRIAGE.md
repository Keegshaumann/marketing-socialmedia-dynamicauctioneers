# Fix-list triage (2026-08-13, 9 agents, read against the code)

| Item | Status | Effort | Where | Notes |
|---|---|---|---|---|
| **1.1** Multi-batch uploads must APPEND, not replace; drag & drop on | partial | M | webapp/routes/gates.py:856-920 (gate2_photo_upload), :767-782 (_photo_list), :731 (_MAX_PH | The SERVER already appends: gate2_photo_upload loads the existing list (`full = _photo_list(_load(db_path, dp))`) and only appends, and same-named files from different folders are  |
| **1.2** Gallery (grid) view of uploaded images | partial | S | webapp/templates/partials/_gate2_photos.html:28-56 (the existing .gallery-grid of tiles);  | A grid already ships: uploaded photos render as a responsive tile grid with a 120px object-fit:cover thumbnail, a LEAD badge, a low-res warning badge with pixel dimensions, and Lea |
| **1.3** Checkbox-style multi-select that survives scrolling / filter | todo | M | webapp/templates/partials/_gate2_photos.html:39-52 (tile actions - per-tile Lead/Remove bu | There is no selection concept anywhere in the app - a grep for 'checkbox' across webapp/templates finds only post.html:94 and settings.html:86, neither photo-related. Two things ar |
| **1.4** Multiple named galleries per property; info pack renders eac | conflict | XL | engine/schema.py:279-287 (Marketing.hero_photo / gallery: Optional[List[str]] - the flat l | Nothing grouped exists today - photos are one ordered list, hero first, and the info pack prints one 'Gallery' section batched 9 to a page. This is a genuine schema change (Marketi |
| **2.1** Generate 3 ad variations per run | todo | L | engine/render/base.py:21-31 (FORMATS has one `demo_ad` key); engine/render/service.py:360- | Today exactly ONE advert is produced per render: `demo_ad`, rendered through the single design on `marketing.template_set`, written to `artifacts/demo_ad.html` and listed once in m |
| **2.2** Fix: headline not showing on generated ads | todo | S | engine/render/templates/ads/hero_overlay.html.j2:13,78-84 (renders place_line/scheme/descr | Confirmed by rendering all four designs: with headline='3 Bedroom Family Home with Separate Flatlet in Morningside', the string appears only in collage. hero_overlay, stats_first a |
| **2.3** Pull suburb info into the headline (e.g. "Morningside, Sandt | partial | M | engine/render/html_backend.py:461-472 _place_line(); :317 vm['place_line']; engine/schema. | The two-part place line already exists and renders on 3 of 4 designs — but its second element is `identity.municipality` with ' Municipality'/' Local' stripped, i.e. an administrat |
| **2.4** Drag & drop to rearrange elements/images within the ad | todo | L | webapp/static/app.js:28-130 (the only drag-drop in the app: file intake into [data-dropzon | Nothing of this exists. Photo order can only be changed by promoting one photo to Lead; element order cannot be changed at all — each design's blocks are fixed markup and the photo |
| **2.5** Swap a single picture in an ad without regenerating the whol | partial | M | webapp/routes/gates.py:922-937 gate2_photo_hero, :940-962 gate2_photo_delete, :855-919 gat | Half true today. You can change the LEAD photo and remove a photo, and pre-approval that re-renders only `demo_ad` (D39) with copy served from cache (D56 measured a design switch a |
| **2.6** Multi-dwelling: each dwelling represented once, correctly | todo | XL | engine/schema.py:142-165 Physical (ONE bedrooms/bathrooms_main_unit/garages), :85-92 Flatl | Root cause confirmed: the record has no concept of a second or third dwelling. Extraction folds every structure into one bedroom/bathroom/garage count plus at most one flatlet, so  |
| **2.7** Preserve source units — ha stays ha, m2 stays m2, always low | todo | M | engine/render/templates/ads/hero_overlay.html.j2:46 (.ho-item text-transform:uppercase) an | Measured in Chromium: all four designs render '±1 228 995M² PROPERTY' / '1 228 995 M²' — the unit is uppercased by text-transform on the stat rows. Two further facts: (a) the ads n |
| **2.8** Never split a number, or a number from its unit, across line | todo | S | engine/render/service.py:83-85 _rand (format(...).replace(',', ' ') — a plain U+0020 betwe | There is no guard anywhere: zero non-breaking spaces in the ad templates, no nowrap binding a number to its unit, and both money and extent formatting use ordinary spaces as thousa |
| **2.9** Email address on ads is a user input field — do not auto-pop | todo | S | engine/render/html_backend.py:42-59 BRAND (email at :47, email_admin at :49), :389-390 (vm | Confirmed hard-coded: every design prints BRAND['email'] = properties@dynamicauctioneers.co.za and there is no input for it anywhere. Note the live mismatch this causes — the team' |
| **2.10** Style the DOP banner as a shield shape with a drop shadow | partial | S | engine/render/templates/ads/feature_list.html.j2:41-44 (.shield, clip-path, no shadow) and | Nothing in this repo is called 'DOP' — I grepped the whole tree. The closest real element is the white shield holding the DS lockup: it EXISTS on feature_list, hero_overlay and sta |
| **2.11** Contact number left side, email right side | todo | S | engine/render/templates/ads/collage.html.j2:59-64 (.ig-contact) and :111-114; feature_list | Confirmed by measurement on all four designs: `.ig-contact` is `display:flex; align-items:center; gap:44px` with no justify-content, so both items pack left — in the 1080px bar the |
| **2.12** Fix padding so no text/elements are cut off at the sides | partial | M | engine/render/templates/ads/collage.html.j2:49-53 (.ig-terms and .ig-tag: white-space:nowr | Reproduced in Chromium with realistic data. In Collage the feature tagline's text box runs to x=1412 on a 1080 canvas and is ellipsised mid-word ('...MODERN KITCHEN WITH GAS HOB /  |
| **3.1** Block info pack generation until OTP (Conditions of Sale) an | todo | L | engine/intake.py:39-81 (marker tables: only LIGHTSTONE/PROPERTY_REPORT/VALUATION), classif | There is no notion of an OTP or a Rates & Taxes document anywhere: intake knows exactly three kinds and completeness is EVM + Property Report only, and the web intake proceeds even |
| **3.2** Extract auction terms from OTP clauses (deposit 3.1, guarant | partial | XL | engine/schema.py:251-263 (SaleProcess carries only terms: List[str] - no deposit/guarantee | Half true today: the terms box renders sale_process.terms from the record (DP3060 carries deposit %, commission %, guarantee days and the occupation line), but those come from the  |
| **3.3** Physical address from Lightstone (not the ad), no duplicated | todo | M | engine/render/templates/info_pack.html.j2:489-511 (Physical Address block 491-499, Legal A | Doubling is real and reproducible on the golden record: street_address = 'Unit 1 Kyalami, 40 Topham Road, Pelham North, Pietermaritzburg' then a separate 'Pelham North' line, while |
| **3.4** Freehold / tenure pulled from the wrong source - must come f | conflict | M | engine/extract.py SYSTEM_PROMPT:155-160 ('The Lightstone EVM ... wins for identity (title  | Nothing today can override tenure either: identity.title_type is not in the gate-2 editable set (gates.py:968-980) and only Physical fields get the gate-1 source picker, so a wrong |
| **3.5** Leave out GPS co-ordinates entirely | todo | S | engine/render/templates/info_pack.html.j2:541-543 (renders 'Coordinates: approx. {{ vm.gps | The pack is the only artifact that prints coordinates - a grep of every template shows gps_str used nowhere else - so this is deleting three template lines and, optionally, the vie |
| **3.6** Remove 'Schedule of Portions'; follow the reference info pac | todo | S | engine/render/templates/info_pack.html.j2:552-588 (the whole '2b SCHEDULE OF PORTIONS' blo | The schedule page is an engine invention, not a reference-pack feature: docs/INFO-PACK-PLAYBOOK.md:60-69 page order has no such page, and §6 (:150-164) describes multi-property pac |
| **3.7** Multi-portion properties: each portion gets its own Property | todo | XL | engine/schema.py:95-110 (Portion carries label/erf/size_m2/title_deed_no/note only - no ro | This is the same gap SPEC D61 logs as owed: 'the multi-unit pack (three unit columns to a page, per-unit extents and feature lists), which needs per-unit physical detail on the rec |
| **4.1** Follow DP2987 layout (cover, TOC, Property Overview, Descrip | conflict | XL | engine/render/templates/info_pack.html.j2 (whole file: @page L218, .page 297x210mm L234-23 | Today's pack renders cover, location details, schedule of portions, features, gallery, investment highlights, closing - so only Cover, Photographic Overview (Gallery), Investment H |
| **4.2** Rename 'Also included' to 'Additional Features' | todo | S | engine/render/templates/info_pack.html.j2:618 - {% if loop.first %}<h3 class="colhead">{{  | One string. Note the heading only prints when the record has no features_complex; when it does (the DP3060 golden case) the column reads 'EXTERNAL AND SHARED:' - I confirmed that i |
| **4.3** Remove the 'Accommodation as inspected. All sizes...' discla | todo | S | engine/render/templates/info_pack.html.j2:623-625 (the .footline div), CSS .footline L372- | The line reads 'Accommodation as inspected - all sizes and finishes to be confirmed on due diligence' and prints uppercase on the first features page (verified in a rendered PDF).  |
| **4.4** Split improvements into grouped pages (Residential / Commerc | partial | XL | engine/render/templates/info_pack.html.j2:116-131 (fm_all/fc_all flattened into one feat_a | Pagination and the icon grid already exist, but the split is arithmetic (feat_per_page) and never by theme, and the layout is glyph-left/label-right rows whereas DP2987 is a centre |
| **4.5** Remove the background image; single flat background layer; a | partial | M | engine/render/rasterize.py:107-157 (html_to_pdf, Chromium print path); engine/render/html_ | I rendered the golden pack and inspected the PDF: text IS real text (embedded Montserrat subsets, Type0, fully extractable) and the frame, swoosh and feature glyphs are vector path |
| **4.6** Viewing section: per arrangement / set viewing time (date+ti | todo | L | engine/schema.py:245-249 (Viewing holds only by_appointment + contact_public + the strippe | Today viewing is one boolean that renders 'Strictly by appointment' or 'By arrangement with Dynamic Auctioneers', and there is no viewing control anywhere in the web app - marketin |
| **5.1** Restyle Investment Highlights to look nicer (two-column tabl | partial | S | engine/render/templates/info_pack.html.j2 - CSS `.hl` block lines 396-399 (`border: 1px so | A bordered two-column table already exists (playbook 5.5 shape: narrow bold left column, wide right column, black hairlines) but it has none of the DP2987 finish: no alternating cr |
| **5.2** "How it is offered" (sale basis / offers-from) is NOT an inv | todo | S | engine/render/templates/info_pack.html.j2:135-139 (the `is_auction` branch appends ('Offer | Confirmed the row is exactly what the client says it is and the DP2987 reference has no such row (its 13 rows are all property merits). Deleting it is a two-line template change, B |
| **5.3** Give highlights an investor spin: projected ROI if rented, f | conflict | XL | engine/render/copy.py - CopyBundle lines 79-93 (no highlights field today) and SYSTEM_PROM | Split the item into three. (a) "Derive good things to say from the rest of the property data" is half done already - the `hl` list builds up to 10 rows from record fields, and the  |
| **6.1** Ratio/size 1200x1600 px, no property picture on the board | todo | M | engine/render/templates/auction_board.html.j2 — canvas comment L5-9, `.board { width:1620p | Today the board is 1620x1080 (3:2 LANDSCAPE) and it DOES put the hero photo in a gold-framed 600px column (a `nophoto` fallback narrows it to 306px, it is not the intended state).  |
| **6.2** Reference dropdown: master ref / property ref / both; master | partial | M | engine/render/templates/auction_board.html.j2 L126-129 (`MASTER REF` if set, `PROPERTY REF | Half exists: the board already renders both pills, the wording matches the ref ('MASTER REF: G971/2024'). Two gaps. (1) There is no mode selector at all — PROPERTY REF is hardcoded |
| **6.3** QR code on the board, generated via the GHL API, DS logo cen | todo | M | engine/distribute/ghl.py — GHL_API_BASE:49, GHL_MEDIA_UPLOAD_URL:255, posts endpoint L455; | Zero coverage today. The repo's GHL client can upload media and create social posts, nothing else, so 'via the GHL API' cannot be taken at face value without confirming the endpoin |
| **6.4** Board modes: one board = one property, and one board = multi | todo | XL | engine/render/base.py L35-53 (`RenderRequest` carries a single `dp` + a single `public_rec | Single-property is done (that is all the pipeline can do). Multi-property has no foothold: the whole render contract is one record in, one artifact out, and there is no multi-recor |
| **6.5** Industrial/Commercial/Agricultural boards must carry the siz | partial | M | engine/render/templates/auction_board.html.j2:138 (`±{{ vm.size_str }}m² Property`); engin | A size line already renders on every board, so the m2 half is effectively done (`±111m² Property`). Two real gaps. (1) Hectares never appear on the board: `size_ha` exists in the v |
| **6.6** Residential boards always show bedrooms (e.g. 3-BED) | partial | S | engine/render/templates/auction_board.html.j2:139 (`{% if vm.beds %}` stat row) and L134 ( | The data is there and beds do render, but in two forms neither of which matches the reference: a stat row reading '3 Bedrooms' with a bed icon, and a descriptor line '3 Bedroom Hom |
| **6.7** Board layout per the example (black header ONLINE | DAY / DA | todo | L | engine/render/templates/auction_board.html.j2 in full — gold ref bar L51-52/126-129, left  | The current board is a different design end to end: landscape, photo-led, gold metallic chrome, auction line in a bordered money box near the FOOT, gold contact bar with phone + em |
