.env
engine.db
engine.db-*
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
node_modules/
*.log

# local demo scaffolding (webapp Canva demo)
.demo-run/
# Generated render output: manifests carry live Canva edit-capability URLs and
# machine-absolute paths; artifacts are rebuilt from the record on demand.
DP*/artifacts/
DP*/packs/
# Ad-design thumbnails, generated on demand from sample data (D41).
.ad-thumbs/
# Ad-design source exports (inputs; I rebuild them into templates/ads/*.html.j2).
designs/incoming/*
!designs/incoming/README.md

# Cached Claude results (extraction / market research), regenerable
.ai-cache/
